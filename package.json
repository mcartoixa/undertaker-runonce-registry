{
  "name": "undertaker-runonce-registry",
  "type": "module",
  "version": "0.0.1",
  "keywords": [
    "gulp",
    "gulp-registry"
  ],
  "files": [
    "./index.js"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:mcartoixa/undertaker-runonce-registry.git"
  },
  "scripts": {
    "build:analyze": "gulp analyze",
    "build:build": "gulp build",
    "build:clean": "gulp clean",
    "build:compile": "gulp compile",
    "build:package": "gulp package",
    "build:prepare": "gulp prepare",
    "build:rebuild": "gulp rebuild",
    "build:release": "gulp release",
    "build:test": "gulp test",
    "eslint": "eslint --cache --cache-location ./.tmp/cache/eslint/",
    "test": "jest --cache --coverage"
  },
  "dependencies": {
    "last-run": "^2.0.0",
    "undertaker-registry": "^2.0.0"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.28.5",
    "@eslint/compat": "^1.4.0",
    "@eslint/js": "^9.38.0",
    "del": "^8.0.1",
    "eslint": "^9.38.0",
    "eslint-plugin-jest": "^29.0.1",
    "gulp": "^5.0.1",
    "gulp-eslint-new": "^2.5.0",
    "jest": "^30.2.0",
    "jest-cli": "^30.2.0",
    "jest-html-reporter": "^4.3.0",
    "jest-junit": "^16.0.0"
  }
}
